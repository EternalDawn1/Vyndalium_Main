@inherits Panel
@using GeneralGame
@namespace GeneralGame.HUD
<root>
    <button class="exit-button" @onclick="@(() => shopStorage.CloseQuest())"></button>
    <div class="quests">
        <div class="quests__header">
            <h1>Quests</h1>
        </div>
        <div class="quests__header">
            <h1>Active Quests</h1>
        </div>
        <div class="quests-content">
            @foreach (var quest in shopStorage.QuestManager.ActiveQuests)
            {
                <div class="box">
                    <div class="name">@quest.Title</div>
                    <div class="description">@quest.Description</div>
                    @if (quest.IsCompleted)
                    {
                        <div class="status">Completed</div>
                    }
                    else
                    {
                        <div class="status">In Progress</div>
                    }
                </div>
            }
        </div>
    </div>
</root>

@code {
    public static Quests Instance { get; private set; }
    private QuestInteraction shopStorage;

    public Quests()
    {
        Instance = this;
        shopStorage = new QuestInteraction();
    }
}